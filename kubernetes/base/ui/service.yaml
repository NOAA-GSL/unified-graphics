apiVersion: v1
kind: Service
metadata:
  labels:
    app: graphics-ui
  name: graphics-ui
spec:
  ports:
  - port: 80
    targetPort: 80 # Port to forward to in the pod
    protocol: TCP
    name: http
  selector:
    app: graphics-ui
  type: ClusterIP
